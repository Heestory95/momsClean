<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.mom.admin.mapper.user.UserMapper">

	<!-- 목록 조회 -->
	<select id="list" resultType="User">
		<![CDATA[
		SELECT user_no, user_id, user_name, user_phone, user_email, email_auth, reg_date, user_division
		FROM (SELECT user_no, user_id, user_name, user_phone, user_email, email_auth, reg_date, user_division FROM user_tbl)
		WHERE user_no > 0
		ORDER BY user_no ASC
		]]>
	</select>
	
	<!-- 회원목록 전체 건수 조회 -->
	<select id="count" resultType="int">
		<![CDATA[
			SELECT count(user_no)
			FROM user_tbl
			WHERE user_no > 0
		]]>
	</select>
</mapper>